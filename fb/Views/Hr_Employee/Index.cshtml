@*@model IEnumerable<fb.Models.Entites.Hr_Employee>

<div class="container p-3 ">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary"> Employee </h2>
        </div>
        <div class="col-6 text-right">
            <a asp-controller="Employee" asp-action="Create" class="btn btn-primary">
               <i class="fas fa-plus"></i> &nbsp; Create New Employee
            </a>

        </div>
    </div>
    <br /><br />
    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped " style="width:100%">
            <thead>
                <tr>
                 <th>
                    Id
                    </th>
                    <th>
                       DEL_MARK
                    </th>
                     <th>
                       DateAdded
                    </th>
                     <th>
                       DateOfDeletion
                    </th>
                     <th>
                       ShiftId
                    </th>
                     <th>
                       BalanceId
                    </th>
                     <th>
                       DiscountId
                    </th>
                     
                    <th></th>

                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model)
                {
                    <tr>
                        <td width="30%">@obj.Id</td>
                        <td width="30%">@obj.DEL_MARK</td>
                        <td width="30%">@obj.DateAdded </td>
                        <td width="30%">@obj.DateOfDeletion </td>
                        <td width="30%">@obj.ShiftId</td>
                        <td width="30%">@obj.BalanceId </td>
                        <td width="30%">@obj.DiscountId </td>
                        
                       
                        <td class="text-center">
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="Employee" asp-route-Id="@obj.Id" asp-action="Edit" class="btn btn-primary mx-2">
                                <i class="fas fa-edit"></i>Edit
                            </a>
                            <a asp-controller="Employee" asp-route-Id="@obj.Id" asp-action="Delete" class="btn btn-danger mx-2">
                                <i class="far fa-trash-alt"></i> Delete 
                            </a>
                        </div>
                    </td>
                </tr>
                }
            </tbody>
        </table>
    }
    else
     {
       <p> No Employees Exists </p>
    }
    
</div>

*@